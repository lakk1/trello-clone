const Header = () => {
  return (
    <div className="header">
      <h1>Trello Clone</h1>
      <button className="btn_signout">Sign out</button>
    </div>
  );
};

export default Header;
